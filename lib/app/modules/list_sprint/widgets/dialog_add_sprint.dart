import 'package:flutter/material.dart';

class DialogAddSprint extends StatelessWidget {
  const DialogAddSprint({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Dialog();
  }
}
