import 'package:flutter/material.dart';

import '../models/sprint.dart';

class CardSprint extends StatelessWidget {
  final Sprint sprint;

  const CardSprint({Key? key, required this.sprint}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
