import 'package:flutter/material.dart';

class CardSprint extends StatelessWidget {
  const CardSprint({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
